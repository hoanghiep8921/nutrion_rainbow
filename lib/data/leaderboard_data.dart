// Deprecated: the hard-coded "friends" leaderboard has been removed.
//
// The leaderboard is now built entirely from real users who have signed in on
// this device — see `AppState.leaderboard` and the persisted `_leaderboard`
// registry (userName -> XP). This file is intentionally left empty.
